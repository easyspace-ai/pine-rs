{
  "name": "pine-tv-browser-tests",
  "version": "1.0.0",
  "description": "Browser-based tests for pine-tv multi-pane display",
  "private": true,
  "scripts": {
    "test": "jest --verbose",
    "test:debug": "jest --verbose --detectOpenHandles"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "playwright": "^1.40.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 60000,
    "verbose": true
  }
}
