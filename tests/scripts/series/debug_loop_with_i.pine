//@version=6
// Debug loop with variable i accessing close[i]
indicator("Debug Loop with i", shorttitle="DebugLoopI", overlay=true)

x = 0.0
for i = 0 to 4
    // Just accessing close[i]
    dummy = close[i]
    x := x + 1.0

plot(x, title="x", color=color.red, display=display.none)
