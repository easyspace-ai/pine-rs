//@version=6
// Debug loop variables
indicator("Debug Loop", shorttitle="DebugLoop", overlay=true)

// Just a simple loop that increments count 5 times
count = 0
for i = 0 to 4
    count := count + 1

plot(count, title="count", color=color.red, display=display.none)
