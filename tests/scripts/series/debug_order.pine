//@version=6
// Debug the order of operations
indicator("Debug Order", shorttitle="DebugOrder", overlay=true)

x = 0.0
for i = 0 to 4
    x := x + 1.0
    // Now access close[i] AFTER incrementing x
    dummy = close[i]

plot(x, title="x", color=color.red, display=display.none)
