//@version=6
// Debug float only, no series access
indicator("Debug Float Only", shorttitle="DebugFloat", overlay=true)

floatCount = 0.0
for i = 0 to 4
    floatCount := floatCount + 1.0

plot(floatCount, title="floatCount", color=color.green, display=display.none)
