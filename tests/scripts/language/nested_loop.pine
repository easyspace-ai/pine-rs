//@version=6
indicator("Nested Loop Test")

// Nested for loops to compute a weighted sum
result = 0.0
for i = 1 to 3
    for j = 1 to 3
        result := result + i * j

// Result should be (1+2+3) * (1+2+3) = 36 on every bar
plot(result, title="Nested Result")
