P4 焦点：VM 纵深 + 门禁不退化 + pine-tv Phase A 已完成

门禁（须长期全绿）：
- ./scripts/dev_verify.sh --full（fmt、clippy -D warnings、workspace test、run_golden、vm_run_golden、phase_acceptance）

已稳定（见 completed_tasks.json 中 P4-T4 至 P4-T11 + pine-tv Phase A；并经本地复验）：
- pine-vm：复合 series 与执行器变量状态不被 bar 内临时求值串扰；vm_golden_test 16/16；DEBUG 由 `PINE_VM_DEBUG` 控制；测试代码无编译警告。
- pine-vm vs pine-eval：已建立脚本级 parity 测试，当前覆盖 **16 支真实脚本**（所有 tests/scripts/stdlib/ta/ 下的脚本），逐 bar plot 输出一致，包含 tuple 返回值与复杂指标计算场景。**VM parity 已达成 100% 覆盖所有黄金 ta.* 脚本**。
- pine-tv：`data::loader::tests::test_generate_sample_data` 与全量门禁一致通过。
- 仓库卫生：根目录 debug_ast.rs 已删除；three/ 目录已文档化为外部参考项目（见 three/README.md）。
- **pine-tv Phase A 已完成**：浏览器访问 http://localhost:7070 可用，Monaco 编辑器 + lightweight-charts 图表，内置 SMA/RSI/EMA 示例，/api/run 端点正常工作（Axum + pine-eval），cargo test -p pine-tv 通过。

最近一次复验命令（均 exit 0，无警告）：
- cargo build -p pine-vm --tests（无 dead_code 警告）
- cargo test -p pine-vm --test vm_script_parity（16/16 通过）
- cargo test -p pine-tv（5 passed）
- ./scripts/dev_verify.sh --full
- curl http://localhost:7070/api/run（API 正常返回）

里程碑达成：
- **VM vs pine-eval parity 已完成所有 ta.* 黄金脚本的覆盖**，证明 pine-vm 在真实指标计算场景下与 pine-eval 输出完全一致。
- **pine-vm 测试代码质量提升**，消除所有编译警告。
- **仓库卫生完成**，符合 AGENTS.md §3 要求。
- **pine-tv Phase A 完成**，验证壳可消费内核输出，符合 AGENTS.md §8 第三层要求。

下一步（主线，按 AGENTS.md 第 8 节第二层与第三层）：
- 选项 1：探索更复杂的控制流与 series 操作场景（如嵌套 UDF、复杂 for 循环等）。
- 选项 2：考虑 Phase 4 其他能力扩展（UDT、array.*、map.*、str.* 等），需独立验收。
- 选项 3：pine-tv 产品化增强（三栏布局、更多示例、实时数据等），与 Phase 4 可并行。

次要（有空再做）：
- 文档更新：在 docs/ 中记录 VM parity 测试框架与覆盖情况。
- pine-tv 增强：更多图表类型、策略回测可视化等。
