Phase 4 当前任务（2026-04-05）

主任务
- 恢复并维持 `./scripts/dev_verify.sh --full` 全绿
- 在不放松门禁的前提下，把 `pine-vm` 提升到可替换标准
- 按官方 Pine Script v6 口径继续完成语法与指标基建收敛

当前口径
- `pine-tv` 仍是验证壳，不作为阶段完成标准
- strategy 仅保留已完成能力与必要回归，不再作为当前主焦点
- VM 已不再只是实验后端；后续新增脚本与关键修复默认同时要求 eval / VM 一致
- 在正式切换主引擎前，入口层仍保留 eval 作为对照路径；CLI / API 已具备显式切换基础

本次核对后的事实
- Phase 3 收尾项并非待做：黄金配对、`for` inclusive 回归、`ta.highest/lowest/highestbars/lowestbars` 黄金均已在仓库中
- `ta.*`、`array.*`、`str.*`、`strategy.*` 的实际覆盖已明显高于旧估计
- `pine-vm` 已通过同批黄金与 script parity：44 / 44 全绿
- 当前剩余工作从“补明显错误”转为“补默认切换前的覆盖厚度与回退验证”
- 覆盖文档与阶段文档仍未充分反映 VM 现状与后续切换门槛

当前验收标准
- `./scripts/dev_verify.sh --full` 通过
- `cargo test -p pine-vm --test vm_golden_test --test vm_script_parity` 通过
- `docs/V6_ALIGNMENT.md`、`docs/API_COVERAGE.md`、`docs/FUNCTION_COVERAGE.md` 对当前能力描述一致
- VM 可替换标准明确且可追溯
- 入口层已具备“双路可切换、单路可回退”的验证基础

下一批执行
- Batch 1：把 VM 可替换标准写入任务与对齐文档
- Batch 2：把新增关键脚本默认纳入 VM 回归，继续补厚默认切换前覆盖
- Batch 3：继续补语义或语法剩余缺口，优先 `type prefix`、对象类型名、脚本入口语义边界
- Batch 4：在覆盖厚度满足后，评估将 CLI / API 默认执行路径切到 VM

当前最高优先级
- 全量验证门禁恢复稳定
- VM 黄金与 parity 不回退
- 文档口径、状态文件、实际回归三者一致
- 为 VM 成为主路径建立明确切换门槛

参考文档
- docs/V6_ALIGNMENT.md
- docs/API_COVERAGE.md
- docs/FUNCTION_COVERAGE.md
